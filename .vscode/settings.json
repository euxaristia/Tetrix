{
    // Platform-specific LLDB library configuration
    // Linux: Uses system LLDB library (prevents liblldb.so.backup errors)
    // Windows: Comment out the line below - CodeLLDB uses its bundled LLDB library on Windows
    "lldb.library": "/usr/lib/liblldb.so"
}

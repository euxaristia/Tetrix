module CSDL3 {
    // Direct SDL3 imports - no C wrapper needed
    header "include/SDL3/SDL.h"
    link "SDL3"
    export *
}

// PulseAudioWrapper functions are accessed via @_silgen_name in Swift
// No module needed - avoids linking issues on Windows
